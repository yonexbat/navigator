import { ContentHostDirective } from './content-host.directive';

describe('ContentHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentHostDirective();
    expect(directive).toBeTruthy();
  });
});
